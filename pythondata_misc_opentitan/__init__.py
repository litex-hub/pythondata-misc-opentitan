import os.path
__dir__ = os.path.split(os.path.abspath(os.path.realpath(__file__)))[0]
data_location = os.path.join(__dir__, "resources")
src = "https://github.com/lowRISC/opentitan"

# Module version
version_str = "0.0.post12653"
version_tuple = (0, 0, 12653)
try:
    from packaging.version import Version as V
    pversion = V("0.0.post12653")
except ImportError:
    pass

# Data version info
data_version_str = "0.0.post12511"
data_version_tuple = (0, 0, 12511)
try:
    from packaging.version import Version as V
    pdata_version = V("0.0.post12511")
except ImportError:
    pass
data_git_hash = "de4cb1bb9e7b707a3ca2a6882d83af7ed2aa1ab8"
data_git_describe = "v0.0-12511-gde4cb1bb9"
data_git_msg = """\
commit de4cb1bb9e7b707a3ca2a6882d83af7ed2aa1ab8
Author: Alphan Ulusoy <alphan@google.com>
Date:   Sat Jun 11 11:52:02 2022 -0400

    [sw/silicon_creator, sw/test_rom] Use autogen flash_ctrl.hjson
    
    This commit updates mask_rom and test_rom dependencies to use the
    autogenerated `flash_ctrl.hjson` as opposed to the hand-written one
    since the former is updated by `prepare_sw.py` while the latter is not.
    This discrepancy causes the erase verification stage of the bootstrap to
    fail on CW305 since we try to verify 512 KiB/bank while english
    breakfast has only 32 KiB/bank.
    
    Signed-off-by: Alphan Ulusoy <alphan@google.com>

"""

# Tool version info
tool_version_str = "0.0.post142"
tool_version_tuple = (0, 0, 142)
try:
    from packaging.version import Version as V
    ptool_version = V("0.0.post142")
except ImportError:
    pass


def data_file(f):
    """Get absolute path for file inside pythondata_misc_opentitan."""
    fn = os.path.join(data_location, f)
    fn = os.path.abspath(fn)
    if not os.path.exists(fn):
        raise IOError("File {f} doesn't exist in pythondata_misc_opentitan".format(f))
    return fn
