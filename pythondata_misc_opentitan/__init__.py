import os.path
__dir__ = os.path.split(os.path.abspath(os.path.realpath(__file__)))[0]
data_location = os.path.join(__dir__, "resources")
src = "https://github.com/lowRISC/opentitan"

# Module version
version_str = "0.0.post15016"
version_tuple = (0, 0, 15016)
try:
    from packaging.version import Version as V
    pversion = V("0.0.post15016")
except ImportError:
    pass

# Data version info
data_version_str = "0.0.post14874"
data_version_tuple = (0, 0, 14874)
try:
    from packaging.version import Version as V
    pdata_version = V("0.0.post14874")
except ImportError:
    pass
data_git_hash = "531012ec5e26138c845aa1984591a95c396dc9d9"
data_git_describe = "v0.0-14874-g531012ec5e"
data_git_msg = """\
commit 531012ec5e26138c845aa1984591a95c396dc9d9
Author: Timothy Trippel <ttrippel@google.com>
Date:   Thu Oct 20 18:29:06 2022 -0700

    [dif] support interrupt types in autogenerated DIFs
    
    This updates the DIF autogeneration templates and autogenerated DIFs to
    add a DIF that allows users to query the interrupt type (event or
    status type). This is in response to #15580.
    
    Note, this depends on #15580.
    
    Signed-off-by: Timothy Trippel <ttrippel@google.com>

"""

# Tool version info
tool_version_str = "0.0.post142"
tool_version_tuple = (0, 0, 142)
try:
    from packaging.version import Version as V
    ptool_version = V("0.0.post142")
except ImportError:
    pass


def data_file(f):
    """Get absolute path for file inside pythondata_misc_opentitan."""
    fn = os.path.join(data_location, f)
    fn = os.path.abspath(fn)
    if not os.path.exists(fn):
        raise IOError("File {f} doesn't exist in pythondata_misc_opentitan".format(f))
    return fn
