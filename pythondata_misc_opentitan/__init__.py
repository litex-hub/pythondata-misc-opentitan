import os.path
__dir__ = os.path.split(os.path.abspath(os.path.realpath(__file__)))[0]
data_location = os.path.join(__dir__, "resources")
src = "https://github.com/lowRISC/opentitan"

# Module version
version_str = "0.0.post9714"
version_tuple = (0, 0, 9714)
try:
    from packaging.version import Version as V
    pversion = V("0.0.post9714")
except ImportError:
    pass

# Data version info
data_version_str = "0.0.post9592"
data_version_tuple = (0, 0, 9592)
try:
    from packaging.version import Version as V
    pdata_version = V("0.0.post9592")
except ImportError:
    pass
data_git_hash = "61a8873335fa9bb78052a4e28b5455bddf1127bf"
data_git_describe = "v0.0-9592-g61a887333"
data_git_msg = """\
commit 61a8873335fa9bb78052a4e28b5455bddf1127bf
Author: Drew Macrae <drewmacrae@google.com>
Date:   Sat Jan 22 12:36:01 2022 -0800

    [bazel] fix dependency in mask_rom_epmp_test
    
    bazel has a rule to build the autogenerated pwrmgr_reg but not the hjson
    file in the parent directory which might be older.
    
    Signed-off-by: Drew Macrae <drewmacrae@google.com>

"""

# Tool version info
tool_version_str = "0.0.post122"
tool_version_tuple = (0, 0, 122)
try:
    from packaging.version import Version as V
    ptool_version = V("0.0.post122")
except ImportError:
    pass


def data_file(f):
    """Get absolute path for file inside pythondata_misc_opentitan."""
    fn = os.path.join(data_location, f)
    fn = os.path.abspath(fn)
    if not os.path.exists(fn):
        raise IOError("File {f} doesn't exist in pythondata_misc_opentitan".format(f))
    return fn
